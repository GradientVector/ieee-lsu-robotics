#summary the high-level function encompassing what the execution phase does


goTo(Point destination) {
    if(destination is imaginary) 
-------deadReckonTo(destination);
        else 
----honeInOn(destination);
}






#authors: Andrew, Ravi